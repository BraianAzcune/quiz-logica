Quiz de logica proposicional

## configuracion

instalar dependencias `pnpm install`

ejecutar con `pnpm dev`

buidear con `pnpm build`

el resultado del build se guarda en la carpeta docs/, para ser utilizado por github pages.

